#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplie two integer.
 *
 * @a : number two to multiplie
 *
 * @b : number one to multiplie
 *
 * Return: Always 0.
 */

int mul(int a, int b)
{
return (a * b);
}
