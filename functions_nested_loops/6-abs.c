#include "main.h"
/**
 * _abs - Compute the absolute value of an integer
 * @a: The integer to compute
 *
 * Return: The absolute value of a
 */
int _abs(int a)
{
if (a < 0)
{
return (-a);
}
return (a);
}
