#include "main.h"
/**
 * _puts - check the code
 * @: varioable
 * Return: Always 0.
 */
void _puts(char *str)
{
int _putchar(char c);
int i;
for (i = 0; str[i] != '\0'; str++)
_putchar(str[i]);
_putchar('\n');
}
