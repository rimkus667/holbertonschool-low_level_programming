#include "main.h"
0;276;0c/**
 * _puts_recursion - check the code
 * @s : var
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
if (*s == '\0')
{
return;
}
_print_rev_recursion(s + 1);
putchar(*s);
}
